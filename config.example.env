# Sovereign AI Kit Configuration
# Copy this file to ~/.sovereign-ai/config.env and customize

# ── Paths ──────────────────────────────────────────────────
# Root directory for all kit data
SAK_HOME="${HOME}/.sovereign-ai"

# Sandbox boundary for file operations (AI can only write inside this)
SAK_SANDBOX_PATH="${SAK_HOME}"

# SQLite database location for sovereign memory
SAK_DB_PATH="${SAK_HOME}/memory/cache.db"

# Directory for learnings markdown files
SAK_LEARNINGS_DIR="${SAK_HOME}/history/learnings"

# Directory for session logs
SAK_SESSIONS_DIR="${SAK_HOME}/history/sessions"

# Identity files directory
SAK_IDENTITY_DIR="${SAK_HOME}/identity"

# ── Browser Automation ─────────────────────────────────────
# HTTP daemon port (extension polls this)
SAK_BROWSER_PORT=19222

# Browser skill directory (where daemon + extension live)
SAK_BROWSER_SKILL_DIR="${SAK_HOME}/browser"

# ── Logging ────────────────────────────────────────────────
# Log level: debug, info, warn, error
SAK_LOG_LEVEL="info"

# ── Optional: Long-Term Memory Plugin ─────────────────────
# Plugin name: "" (none), "pieces", or path to custom plugin
SAK_LTM_PLUGIN=""

# Path to LTM query script (if using a plugin)
SAK_LTM_SCRIPT=""

# ── Identity (used by generate_claude_md.py) ───────────────
# Your AI instance's name
SAK_AI_NAME="Assistant"

# Comma-separated tone descriptors
SAK_IDENTITY_TONE="precise, honest, curious"
